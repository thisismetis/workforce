<div>
  <p><strong><%= "#{topic.category}: " %><%= topic.rating %></strong>
    <%= link_to "edit", edit_topic_path(topic) %> |
    <%= link_to "delete", topic_path(topic), method: :delete, confirm: "Are you sure?"  %>
  </p>

  <p>Notes: <%= topic.note %></p>
</div>